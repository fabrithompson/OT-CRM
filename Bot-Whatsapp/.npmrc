engine-strict=true
auto-install-peers=true
strict-peer-dependencies=false

# Baileys depende de libsignal vía git-repository (fork oficial de WhiskeySockets).
# pnpm 11.1.1 espera el nombre camelCase aquí (kebab-case no lo respeta).
blockExoticSubdeps=false
